/**
 * Single source of truth for article body typography.
 * Paragraph spacing (prose-p:my-8) is defined here only, so every
 * article page renders identical gaps between paragraphs.
 */
export const articleProse =
  "prose prose-neutral max-w-none prose-headings:font-serif-display prose-p:leading-relaxed prose-p:text-[17px] prose-p:my-8";
